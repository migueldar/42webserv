- maxBodySize > locationNotFound > redir > methods > not found
- cuando el cgi peta no deberia petar el webserv
- los fds no los estamos liberando bien
- comprobar si se esta recibiendo todo el archivo
- porque peta con 10000
? en la config file permitir tabs

GET
- devolver el archivo
POST
- si la localizacion es cgi(200) o permite update de archivos(201)
DELETE
- cargarnos el archivo