# al poner de metodo post, entiendo que solo deberia permitirse post, sin embargo, se permiten post y delete
server {
    port 11;
    server_name a;

    location / {
        index a.html
        methods POST
    }
}
