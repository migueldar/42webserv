events {
    worker_connections 1024;
}

http {
    server {
        listen 9090;
        server_name localhost;

        location / {
            root /var/www/html; 
            index index.nginx-debian.html;
        }
    }
}